<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 404 30" style="background: transparent;">
  <style>
    svg {
      --off: #94a3b8;
      --blue: #0284c7;
      --cyan: #0891b2;
      --green: #15803d;
      --led-dim: #cbd5e1;
    }
    @media (prefers-color-scheme: dark) {
      svg {
        --off: #6e7f96;
        --blue: #388bfd;
        --cyan: #00f2fe;
        --green: #3fb950;
        --led-dim: #33415a;
      }
    }
    @keyframes synActive {
      0%, 25% { fill: var(--blue); }
      25.1%, 100% { fill: var(--off); }
    }
    @keyframes arrow1Active {
      0%, 15% { fill: var(--off); }
      15.1%, 35% { fill: var(--cyan); }
      35.1%, 100% { fill: var(--off); }
    }
    @keyframes synAckActive {
      0%, 35% { fill: var(--off); }
      35.1%, 60% { fill: var(--cyan); }
      60.1%, 100% { fill: var(--off); }
    }
    @keyframes arrow2Active {
      0%, 50% { fill: var(--off); }
      50.1%, 70% { fill: var(--blue); }
      70.1%, 100% { fill: var(--off); }
    }
    @keyframes ackActive {
      0%, 60% { fill: var(--off); }
      60.1%, 85% { fill: var(--blue); }
      85.1%, 100% { fill: var(--off); }
    }
    @keyframes arrow3Active {
      0%, 75% { fill: var(--off); }
      75.1%, 90% { fill: var(--green); }
      90.1%, 100% { fill: var(--off); }
    }
    @keyframes establishedActive {
      0%, 85% { fill: var(--off); }
      85.1%, 98% { fill: var(--green); }
      98.1%, 100% { fill: var(--off); }
    }
    @keyframes blinkLED {
      0%, 85% { fill: var(--off); }
      85.1%, 91% { fill: var(--green); }
      91.1%, 97% { fill: var(--led-dim); }
      97.1%, 100% { fill: var(--off); }
    }
    .syn { animation: synActive 6s infinite; }
    .arrow1 { animation: arrow1Active 6s infinite; }
    .syn-ack { animation: synAckActive 6s infinite; }
    .arrow2 { animation: arrow2Active 6s infinite; }
    .ack { animation: ackActive 6s infinite; }
    .arrow3 { animation: arrow3Active 6s infinite; }
    .established { animation: establishedActive 6s infinite; }
    .led { animation: blinkLED 6s infinite; }
  </style>

  <g font-family="'Courier New', Courier, monospace" font-size="16" font-weight="bold" fill="var(--off)">
    <!-- SYN -->
    <text x="22" y="21" text-anchor="middle" class="syn">SYN</text>

    <!-- Arrow 1 -->
    <text x="60" y="21" text-anchor="middle" class="arrow1">➔</text>

    <!-- SYN-ACK -->
    <text x="110" y="21" text-anchor="middle" class="syn-ack">SYN-ACK</text>

    <!-- Arrow 2 -->
    <text x="168" y="21" text-anchor="middle" class="arrow2">➔</text>

    <!-- ACK -->
    <text x="204" y="21" text-anchor="middle" class="ack">ACK</text>

    <!-- Arrow 3 -->
    <text x="242" y="21" text-anchor="middle" class="arrow3">➔</text>

    <!-- LED Dot -->
    <circle cx="268" cy="15.5" r="4" class="led" />

    <!-- ESTABLISHED -->
    <text x="282" y="21" class="established">ESTABLISHED</text>
  </g>
</svg>
